[package]
name = "pellucid-edge-bin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "hosted public API binary — gateway + handlers on :8080"

[lints]
workspace = true

[lib]
name = "pellucid_edge_bin"
path = "src/lib.rs"

[[bin]]
name = "pellucid-edge-bin"
path = "src/main.rs"

[dependencies]
axum = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
rustls = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
pellucid-core = { path = "../pellucid-core" }
pellucid-db = { path = "../pellucid-db" }
pellucid-cache = { path = "../pellucid-cache" }
pellucid-gateway = { path = "../pellucid-gateway" }
pellucid-handlers = { path = "../pellucid-handlers" }
pellucid-ml = { path = "../pellucid-ml" }
# Edge only uses aviationstack from streams; the telegram client
# is opt-in via the `telegram` feature (relay-bin enables it).
# This keeps grammers-client → libsql → bundled SQLite out of
# the edge link, avoiding a "duplicate symbol: sqlite3_*"
# collision with sqlx's libsqlite3-sys.
pellucid-streams = { path = "../pellucid-streams" }

[dev-dependencies]
tokio = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }
tower = { workspace = true }
wiremock = { workspace = true }
