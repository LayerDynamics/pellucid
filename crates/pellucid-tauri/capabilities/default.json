{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set granted to the trusted main window. Subsequent tasks (T1.7, T1.8) extend this with sidecar IPC commands and rotated-token gating.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:window:allow-set-title",
    "core:window:allow-show",
    "core:window:allow-close",
    "core:event:default",
    "core:webview:default",
    "core:app:default"
  ]
}
